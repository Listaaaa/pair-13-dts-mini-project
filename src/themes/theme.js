import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        
          main: '#F2FDFF',
        },
        secondary: {
          main: '#F2FDFF',
        },
      },
);

export default theme;
